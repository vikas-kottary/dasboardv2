package com.people10.dashboard.repository;

import com.people10.dashboard.model.Opco;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OpcoRepository extends JpaRepository<Opco, Long> {
}
